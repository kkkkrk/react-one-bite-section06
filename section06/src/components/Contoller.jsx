const Controller = ({handleInc}) => {
    return(
        <div>
            ???
            {/* <button onClick={() => {
                handleInc(-10);
            }}>-10</button>

            <button onClick={() => {
                handleInc(-1);
            }}>-1</button>

            <button onClick={() => {
                handleInc(1);
            }}>+1</button>

            <button onClick={() => {
                handleInc(10);
            }}>+10</button> */}
        </div>
    )
}
export default Controller;